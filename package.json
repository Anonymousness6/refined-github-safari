{
  "scripts": {
    "test": "xo && ava && npm run build",
    "build:c": "rollup -c node:webext -i src/content.js -o refined-github.safariextension/content.js",
    "build": "run-p build:*",
    "watch": "run-p --continue-on-error --silent 'build:* -- --watch'",
    "release": "webstore upload --source=extension --auto-publish"
  },
  "dependencies": {
    "copy-text-to-clipboard": "^1.0.0",
    "debounce-fn": "^1.0.0",
    "element-ready": "^2.0.0",
    "escape-goat": "^1.1.0",
    "github-injection": "^0.3.0",
    "to-semver": "^1.1.0"
  },
  "devDependencies": {
    "ava": "*",
    "babel-plugin-transform-es2015-modules-commonjs": "^6.24.1",
    "chrome-webstore-upload-cli": "^1.0.0",
    "npm-run-all": "^4.0.2",
    "rollup": "^0.41.6",
    "rollup-config-webext": "^1.1.0",
    "rollup-watch": "^3.2.2",
    "xo": "*"
  },
  "xo": {
    "esnext": true,
    "envs": [
      "browser",
      "webextensions"
    ],
    "rules": {
      "import/no-unassigned-import": 0
    },
    "ignores": [
      "refined-github.safariextension/**"
    ]
  },
  "ava": {
    "files": [
      "test/*.js"
    ],
    "source": [
      "refined-github.safariextension/*.js"
    ],
    "require": [
      "babel-register"
    ]
  },
  "babel": {
    "plugins": [
      "transform-es2015-modules-commonjs"
    ]
  }
}
